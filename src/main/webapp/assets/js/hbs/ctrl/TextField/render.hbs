{{#compare attr.labelAlign "horizontal"}}
<div class="form-group">
    <label for="{{id}}-{{attr.name}}" class="col-sm-2 control-label">{{attr.label}}</label>
    <div class="col-sm-10">
        <input name="{{id}}-{{attr.name}}" id="{{id}}-{{attr.name}}" type="{{attr.type}}" class="form-control" placeholder="{{attr.placeholder}}" maxlength="{{attr.maxlength}}">
    </div>
</div>
{{/compare}}
{{#compare attr.labelAlign "vertical"}}
<div class="form-vertical">
    <label for="{{id}}-{{attr.name}}">{{attr.label}}</label>
    <input name="{{id}}-{{attr.name}}" id="{{id}}-{{attr.name}}" type="{{attr.type}}" class="form-control" placeholder="{{attr.placeholder}}" maxlength="{{attr.maxlength}}">
</div>
{{/compare}}
{{#compare attr.labelAlign "none"}}
<input name="{{id}}-{{attr.name}}" id="{{id}}-{{attr.name}}" type="{{attr.type}}" class="form-control" placeholder="{{attr.placeholder}}" maxlength="{{attr.maxlength}}">
{{/compare}}